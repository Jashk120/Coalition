{
  "name": "coalition-resource-pool",
  "version": "0.1.0",
  "description": "Subgraph Studio subgraph indexing the Coalition ResourcePool on Arc testnet",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/api/v0 coalition-resource-pool"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.34.0"
  }
}
