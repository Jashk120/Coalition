[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.30"
optimizer = true
optimizer_runs = 200
# Circle Contracts (Smart Contract Platform) on Arc rejects Shanghai/PUSH0
# bytecode; Paris keeps the artifact deployable through both Foundry and SCP.
evm_version = "paris"
